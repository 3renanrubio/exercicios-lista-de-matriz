#include <stdio.h>
int main() {

    int matriz[2][2] = {{1, 1}, {1, 1}};
    int i, j;

    printf("Matriz 2x2 com o número 1:\n");
    for (i = 0; i < 2; i++) {
        for (j = 0; j < 2; j++) {
            printf("%d\t", matriz[i][j]);
        }
        printf("\n");
    }

    return 0;
}
